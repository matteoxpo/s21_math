#include <check.h>
#include <math.h>
#include <stdio.h>

#include "s21_math.h"

#test s21_acos_test
for (double i = -1; i <= 1; i += 0.1) {
  ck_assert_double_eq_tol(acosl(i), s21_acos(i), S21_EPSILON * 10);
}
#include <check.h>
#include <math.h>
#include <stdio.h>

#include "s21_math.h"

#test s21_asin_test
for (double i = -1; i <= 1; i += 0.1) {
  ck_assert_double_eq_tol(asinl(i), s21_asin(i), S21_EPSILON);
}